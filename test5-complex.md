# 多种语法混合的复杂输入

## 第一部分：标题与段落

这是一个 **复杂** 的 Markdown 文档，包含 *多种* 格式。

### 子标题

段落中可以同时有 **加粗** 和 *斜体*，甚至可以 __组合__ 使用。

## 第二部分：列表与内联格式

- 列表项 1：包含 **加粗** 和 *斜体* 文本
- 列表项 2：这是一个很长的列表项，里面有 *斜体内容* 和 **加粗内容**，需要测试换行和格式的处理
- 列表项 3：混合使用__下划线加粗__和*星号斜体*

* 另一个列表开始
* 这个列表项有 **加粗**
* 这个列表项有 *斜体*

## 第三部分：特殊情况测试

这是一个包含#号但不是标题的段落，因为#号后没有空格。

这是一个包含超过6个#号的情况：####### 这不是标题。

这是一个包含*号但不是斜体的情况：1 * 2 = 2。

正常的标题：# 这是标题

正常的列表：- 这是列表项